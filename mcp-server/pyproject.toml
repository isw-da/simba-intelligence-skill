[project]
name = "simba-intelligence-mcp"
version = "1.0.0"
description = "MCP server exposing the Simba Intelligence setup knowledge base as tools for Claude Desktop"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.3.0",
]

